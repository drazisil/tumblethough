machine:
  node:
    version: 6.9.1

test:
  pre:
    - ssh -i ~/.ssh/id_CircleCI git@github.com
  post:
    - npm run coverage
    - mkdir $CIRCLE_TEST_REPORTS/junit && cp results.xml $CIRCLE_TEST_REPORTS/junit/
    - cp -r coverage $CIRCLE_TEST_REPORTS/
